spring:
  profiles:
    active: default
  application:
    name: nt-kafka
  cloud:
    config:
      uri: http://172.16.0.36:19001/

server:
  port: 19003

#eureka:
#  client:
#    serviceUrl:
#      defaultZone: http://localhost:8761/eureka

#Logging 配置
logging:
  file: ./logs/Kiseki/${spring.application.name}/out.log
  level:
    com.newtouch.kiseki: INFO
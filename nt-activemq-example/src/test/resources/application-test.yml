spring:
  profiles:
    active: test
  application:
    name: nt-activemq-example
  activemq:
      broker-url: tcp://kiseki1029.com:61616
      user: admin
      password: admin
  jms:
    pub-sub-domain: true #(为true时是topic模式，为false是queue模式)

queue:
  test: mytest